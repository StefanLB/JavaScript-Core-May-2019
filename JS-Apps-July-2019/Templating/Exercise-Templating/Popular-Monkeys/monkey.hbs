{{#each monkeys}}
<div class="monkey">
    <h2>{{name}}</h2>
    <img src="{{image}}" alt="{{image}}">
    <button>Info</button>
    <p class="info" style="display: none" id="{{id}}">{{info}}</p>
</div>
{{/each}}
